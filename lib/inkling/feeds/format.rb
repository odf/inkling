# An Inkling feed format generates an encoded feed from a collection of source objects
module Inkling
  module Feeds
    module Format
      
      # option_attrs = [ :title, :subtitle, :links, :authors, :contributors, :id]
      # attr_accessor *option_attrs
      # 
      # def initialize(sources, options = {})
      #   @sources = sources
      #   @options = options
      # 
      #   @title = @options[:title] || "Inkling Feed"
      #   @subtitle = @options[:subtitle]
      #   @authors = Array(@options[:authors])
      #   @contributors = Array(@options[:contributors])
      #   @domain = @options[:domain] || "localhost.com"
      #   @id = @options[:id] || "#{domain}:2011:inkling/feed/#{title}"
      # end
      # 
      # def generate
      # end
    end
  end
end